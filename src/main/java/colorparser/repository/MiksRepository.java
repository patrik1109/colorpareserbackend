package colorparser.repository;

import colorparser.entities.Miks;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MiksRepository extends JpaRepository<Miks,Integer> {
}
