package colorparser.repository;

import colorparser.entities.Glue;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GlueRepository extends JpaRepository<Glue,Integer> {
}
